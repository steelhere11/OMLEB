---
phase: 03-technician-reporting
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - src/app/tecnico/page.tsx
  - src/app/tecnico/folios/[folioId]/page.tsx
  - src/app/tecnico/reporte/[reporteId]/page.tsx
  - src/app/tecnico/reporte/[reporteId]/report-form.tsx
  - src/app/tecnico/reporte/[reporteId]/equipment-section.tsx
  - src/app/tecnico/reporte/[reporteId]/equipment-entry-form.tsx
  - src/app/tecnico/reporte/[reporteId]/add-equipment-modal.tsx
  - src/components/tecnico/work-type-toggle.tsx
  - src/components/tecnico/bottom-tab-bar.tsx
autonomous: true

must_haves:
  truths:
    - "Technician sees a list of assigned folios with branch name, client, and today's report status"
    - "Tapping a folio creates or finds today's report and navigates to the report form"
    - "Technician can add per-equipment entries with work type toggle (preventivo/correctivo) and text fields"
    - "Technician can add new equipment from the field without leaving the report"
    - "Cuadrilla members see a refresh banner when another member updates the report"
    - "Equipment list pre-populates from the previous day's report when returning to a multi-day folio"
  artifacts:
    - path: "src/app/tecnico/page.tsx"
      provides: "Real folio list with today's report status badges"
      min_lines: 40
    - path: "src/app/tecnico/folios/[folioId]/page.tsx"
      provides: "Folio detail with get-or-create report flow"
      min_lines: 20
    - path: "src/app/tecnico/reporte/[reporteId]/page.tsx"
      provides: "Server component that fetches report data and renders form"
      min_lines: 30
    - path: "src/app/tecnico/reporte/[reporteId]/report-form.tsx"
      provides: "Client component shell with Realtime subscription"
      contains: "use client"
    - path: "src/app/tecnico/reporte/[reporteId]/equipment-section.tsx"
      provides: "Equipment entries list with add/remove"
      contains: "use client"
    - path: "src/app/tecnico/reporte/[reporteId]/equipment-entry-form.tsx"
      provides: "Single equipment entry form with save"
      contains: "saveEquipmentEntry"
    - path: "src/app/tecnico/reporte/[reporteId]/add-equipment-modal.tsx"
      provides: "Modal to create new equipment from field"
      contains: "createEquipoFromField"
    - path: "src/components/tecnico/work-type-toggle.tsx"
      provides: "Preventivo/Correctivo toggle component"
      contains: "preventivo"
  key_links:
    - from: "src/app/tecnico/page.tsx"
      to: "/tecnico/folios/[folioId]"
      via: "Link component href"
      pattern: "href.*tecnico/folios"
    - from: "src/app/tecnico/folios/[folioId]/page.tsx"
      to: "src/app/actions/reportes.ts"
      via: "getOrCreateTodayReport call"
      pattern: "getOrCreateTodayReport"
    - from: "src/app/tecnico/reporte/[reporteId]/report-form.tsx"
      to: "Supabase Realtime"
      via: "channel subscription for postgres_changes"
      pattern: "supabase.*channel"
    - from: "src/app/tecnico/reporte/[reporteId]/equipment-entry-form.tsx"
      to: "src/app/actions/reportes.ts"
      via: "saveEquipmentEntry server action"
      pattern: "saveEquipmentEntry"
    - from: "src/app/tecnico/reporte/[reporteId]/add-equipment-modal.tsx"
      to: "src/app/actions/equipos.ts"
      via: "createEquipoFromField server action"
      pattern: "createEquipoFromField"
---

<objective>
Build the complete technician navigation flow (folio list -> folio detail -> report form) and the equipment entry section of the report. After this plan, technicians can view assigned folios, create daily reports, and add/edit/remove equipment entries with work type and text fields.

Purpose: This is the core product experience — the vertical slice from folio to report to equipment entries. It replaces the placeholder technician page with a fully functional reporting flow.
Output: Working folio list, report creation, and equipment section. Only materials and status/submit remain for Plan 03.
</objective>

<execution_context>
@C:\Users\Leo\.claude/get-shit-done/workflows/execute-plan.md
@C:\Users\Leo\.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-technician-reporting/03-RESEARCH.md
@.planning/phases/03-technician-reporting/03-01-SUMMARY.md

# Existing code to reference
@src/app/tecnico/layout.tsx
@src/app/tecnico/page.tsx
@src/components/tecnico/bottom-tab-bar.tsx
@src/app/actions/reportes.ts
@src/app/actions/equipos.ts
@src/lib/validations/reportes.ts
@src/types/index.ts
@src/types/actions.ts
@src/components/ui/button.tsx
@src/components/ui/input.tsx
@src/components/ui/textarea.tsx
@src/components/ui/select.tsx
@src/lib/supabase/server.ts
@src/lib/supabase/client.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Folio list, folio detail, and report page server component</name>
  <files>
    src/app/tecnico/page.tsx
    src/app/tecnico/folios/[folioId]/page.tsx
    src/app/tecnico/reporte/[reporteId]/page.tsx
    src/components/tecnico/bottom-tab-bar.tsx
  </files>
  <action>
  1. **Replace `src/app/tecnico/page.tsx`** (currently a placeholder empty state) with a real folio list:

     Server Component. Fetch assigned folios using Supabase:
     - Get authenticated user via `createClient()` + `supabase.auth.getUser()`.
     - Query `folios` with join: `select("*, sucursales(nombre, numero, direccion), clientes(nombre)")`.
     - Filter to non-completed folios: `.in("estatus", ["abierto", "en_progreso", "en_espera"])`.
     - Order by `created_at` descending.
     - RLS ensures only assigned folios are returned.

     Also fetch today's reports to show status per folio:
     - Compute `today = new Date().toISOString().split("T")[0]`.
     - Query `reportes` with `.in("folio_id", folioIds).eq("fecha", today)`.
     - Build a Map of folio_id -> report for quick lookup.

     Render as a list of tappable cards. Each card shows:
     - Folio number (numero_folio) as header, bold
     - Branch name and number (sucursales.nombre + numero)
     - Client name (clientes.nombre)
     - Folio status badge (same color-coded pattern as admin: abierto=blue, en_progreso=yellow, en_espera=orange, completado=green)
     - Today's report indicator: if report exists, show small badge "Reporte de hoy: [estatus]"; if not, show "Sin reporte hoy"
     - Link to `/tecnico/folios/${folio.id}`

     Mobile-first: large touch targets (min-h-[72px] per card), rounded-xl borders, shadow-sm, active:bg-gray-50 press state.

     Keep the empty state (no folios assigned) as a fallback — use the existing empty state SVG and messages.

  2. **Create `src/app/tecnico/folios/[folioId]/page.tsx`**:

     Server Component. This is the folio detail / report entry point.
     - Fetch folio with joins: `select("*, sucursales(nombre, numero, direccion), clientes(nombre)")` by params.folioId.
     - If folio not found, show error or redirect.
     - Fetch team members: query `folio_asignados` joined with `users(nombre, rol)` for this folio. Display as "Equipo: [name1], [name2]".
     - Call `getOrCreateTodayReport(params.folioId)` server action.
     - If successful, use `redirect(`/tecnico/reporte/${result.reporteId}`)` from next/navigation.
     - If error, display the error message.

     This page is essentially a pass-through that creates/finds today's report and immediately redirects. The user will briefly see a loading state before redirect.

     Display while loading/creating:
     - Folio header with numero_folio, branch info, client, problem description
     - "Cargando reporte..." or the redirect happens so fast they won't see it

     NOTE: Since this calls a server action on page load, wrap getOrCreateTodayReport call directly in the server component body (it returns data, not ActionState). Use the result to redirect.

  3. **Create `src/app/tecnico/reporte/[reporteId]/page.tsx`**:

     Server Component wrapper that fetches all report data and passes to client form.
     - Fetch the report: `reportes` with joins to `folios(numero_folio, descripcion_problema, sucursal_id, sucursales(nombre, numero), clientes(nombre))`.
     - Fetch equipment entries: `reporte_equipos` with join `equipos(numero_etiqueta, marca, modelo, tipo_equipo)` for this report.
     - Fetch materials: `reporte_materiales` for this report.
     - Fetch available equipment: `equipos` where sucursal_id matches the report's sucursal_id (for the equipment selector dropdown).
     - Fetch team members: `folio_asignados` joined with `users(nombre)` for this report's folio.
     - If report not found, show error.
     - If report estatus is "completado", show read-only indicator (tech cannot edit, but can view).

     Pass all data to `<ReportForm>` client component as props:
     ```
     reporteId, folioNumero, folioDescripcion, sucursalNombre, clienteNombre,
     initialEntries (equipment entries with equipment details),
     initialMaterials,
     availableEquipment,
     teamMembers,
     currentStatus,
     isCompleted (boolean — locks editing)
     ```

  4. **Modify `src/components/tecnico/bottom-tab-bar.tsx`** to hide the tab bar when inside report editing routes.

     Add a pathname check: if `pathname.startsWith("/tecnico/reporte/")`, return `null` (don't render).
     This gives more screen real estate for the report form on mobile.
     The report form will have its own back button navigation.
  </action>
  <verify>
  - `npx tsc --noEmit` passes
  - `npm run build` succeeds
  - Files exist: src/app/tecnico/page.tsx (modified), src/app/tecnico/folios/[folioId]/page.tsx (new), src/app/tecnico/reporte/[reporteId]/page.tsx (new)
  - bottom-tab-bar.tsx hides on /tecnico/reporte/* routes
  </verify>
  <done>
  Complete navigation flow: folio list -> folio detail (creates/finds report) -> report page (fetches all data). Tab bar hidden during report editing.
  </done>
</task>

<task type="auto">
  <name>Task 2: Report form shell with Realtime, equipment section, entry form, add-equipment modal, and work-type toggle</name>
  <files>
    src/app/tecnico/reporte/[reporteId]/report-form.tsx
    src/app/tecnico/reporte/[reporteId]/equipment-section.tsx
    src/app/tecnico/reporte/[reporteId]/equipment-entry-form.tsx
    src/app/tecnico/reporte/[reporteId]/add-equipment-modal.tsx
    src/components/tecnico/work-type-toggle.tsx
  </files>
  <action>
  1. **Create `src/components/tecnico/work-type-toggle.tsx`**:

     "use client" component. A segmented control toggle for Preventivo/Correctivo.
     Props: `name: string`, `value: "preventivo" | "correctivo"`, `onChange: (val) => void`.
     Render as two side-by-side buttons in a rounded container (bg-gray-50 border).
     Active button gets white background with shadow (bg-white shadow-sm text-brand-700).
     Inactive button gets muted text (text-gray-500).
     Include a hidden input with the current value for form submission.
     Large touch targets (py-2.5 minimum). Follow the exact styling from 03-RESEARCH.md Pattern: Mobile-First Toggle Component.

  2. **Create `src/app/tecnico/reporte/[reporteId]/report-form.tsx`**:

     "use client" main report form component. This is the shell that contains all report sections.

     Props interface (from page.tsx server component):
     ```typescript
     interface ReportFormProps {
       reporteId: string;
       folioNumero: string;
       folioDescripcion: string;
       sucursalNombre: string;
       sucursalId: string;
       clienteNombre: string;
       initialEntries: (ReporteEquipo & { equipos: Equipo })[];
       initialMaterials: ReporteMaterial[];
       availableEquipment: Equipo[];
       teamMembers: { nombre: string; rol: string }[];
       currentStatus: ReporteEstatus;
       isCompleted: boolean;
     }
     ```

     **Header section:**
     - Back button (arrow left icon) linking to `/tecnico` (since tab bar is hidden)
     - Folio number as title
     - Branch name and client below

     **Realtime subscription** (useEffect):
     - Import createClient from @/lib/supabase/client.
     - Subscribe to `supabase.channel(`report-${reporteId}`)` with postgres_changes for:
       - UPDATE on `reportes` table filtered by `id=eq.${reporteId}`
       - INSERT/UPDATE/DELETE on `reporte_equipos` filtered by `reporte_id=eq.${reporteId}`
       - INSERT/UPDATE/DELETE on `reporte_materiales` filtered by `reporte_id=eq.${reporteId}`
     - On any event, show a refresh banner: "Un companero actualizo el reporte." with "Actualizar datos" button.
     - Refresh button calls `router.refresh()` and hides the banner.
     - Cleanup: remove channel on unmount.

     **Report info section:**
     - Show folio description (descripcion_problema) in a light info box
     - Show team members list: "Equipo: [name1 (tecnico), name2 (ayudante)]"
     - Show report date (today)

     **Sections rendered:**
     - `<EquipmentSection>` — from equipment-section.tsx (implemented in this task)
     - Materials section placeholder div with text "Materiales — Plan 03" (will be replaced in Plan 03)
     - Status section placeholder div with text "Estatus — Plan 03" (will be replaced in Plan 03)

     **If isCompleted:** Show a yellow banner "Este reporte esta completado. Solo un administrador puede editarlo." Disable all interactive elements.

     **Mobile UX:**
     - Use `scrollIntoView({ behavior: "smooth", block: "center" })` on input focus with 300ms delay (for keyboard animation) — implement as a utility function or inline handler passed to child components.
     - beforeunload guard: track hasUnsavedChanges state, warn on navigation if true.

  3. **Create `src/app/tecnico/reporte/[reporteId]/equipment-section.tsx`**:

     "use client" component. Manages the list of equipment entries for this report.

     Props: `reporteId: string`, `initialEntries`, `availableEquipment: Equipo[]`, `sucursalId: string`, `isCompleted: boolean`.

     State: `entries` array initialized from initialEntries.

     **Add equipment:** A select dropdown (or large button) showing available equipment from the branch. When selected, call `saveEquipmentEntry(reporteId, null, null, formData)` server action to create a new entry with default tipo_trabajo "preventivo". On success, add the new entry to the local entries array. Use useTransition for non-blocking UI.

     Filter the select to exclude equipment already added (by equipo_id).

     **"Agregar equipo nuevo" button:** Opens the add-equipment modal for on-the-fly creation.

     **Entry list:** Render each entry as an `<EquipmentEntryForm>` component. Accordion-style: show equipment name/tag as header, expand to show full form. This prevents overwhelming the screen with all fields at once.

     **Remove entry:** Each entry has a remove button. Calls `removeEquipmentEntry(entryId)`. Optimistic removal from local state; revert on error.

  4. **Create `src/app/tecnico/reporte/[reporteId]/equipment-entry-form.tsx`**:

     "use client" component for a single equipment entry within the report.

     Props: `entry: ReporteEquipo & { equipos: Equipo }`, `reporteId: string`, `onRemove: () => void`, `isCompleted: boolean`.

     State: local form values for tipo_trabajo, diagnostico, trabajo_realizado, observaciones (initialized from entry).

     Display:
     - Equipment header: numero_etiqueta + marca/modelo if available. Tappable to expand/collapse.
     - WorkTypeToggle for preventivo/correctivo
     - Textarea for diagnostico (label: "Diagnostico", placeholder: "Describe el diagnostico del equipo...")
     - Textarea for trabajo realizado (label: "Trabajo Realizado", placeholder: "Describe el trabajo realizado...")
     - Textarea for observaciones (label: "Observaciones", placeholder: "Observaciones adicionales...")
     - "Guardar" button per entry — calls saveEquipmentEntry server action with the entry's ID (update mode)
     - "Eliminar" button (danger variant, small) — calls onRemove
     - Show "Guardado" indicator after successful save (brief toast or inline text that fades)

     Use useTransition for non-blocking save. Bind saveEquipmentEntry with reporteId and entryId.

     If isCompleted, disable all fields and hide save/remove buttons.

  5. **Create `src/app/tecnico/reporte/[reporteId]/add-equipment-modal.tsx`**:

     "use client" component. Modal/overlay for adding new equipment from the field.

     Props: `sucursalId: string`, `isOpen: boolean`, `onClose: () => void`, `onEquipmentCreated: (equipo: Equipo) => void`.

     Uses useActionState with createEquipoFromField from @/app/actions/equipos.

     Form fields:
     - numero_etiqueta (required) — Input, label "Etiqueta / Numero"
     - marca (optional) — Input, label "Marca"
     - modelo (optional) — Input, label "Modelo"
     - numero_serie (optional) — Input, label "Numero de Serie"
     - tipo_equipo (optional) — Input or Select with common types, label "Tipo de Equipo"
     - Hidden input: sucursal_id

     On success (state.success && state.data), call onEquipmentCreated with the new equipment data, then onClose.
     Show Spanish validation errors from fieldErrors.

     Modal UI: fixed overlay with backdrop blur, centered white card, "Agregar Equipo Nuevo" title, close X button.
     Mobile: modal should take nearly full width (max-w-lg mx-4), respecting safe area.

     Include a small info notice: "Este equipo sera revisado por un administrador."
  </action>
  <verify>
  - `npx tsc --noEmit` passes
  - `npm run build` succeeds
  - All 5 files created: report-form.tsx, equipment-section.tsx, equipment-entry-form.tsx, add-equipment-modal.tsx, work-type-toggle.tsx
  - report-form.tsx imports and renders EquipmentSection
  - equipment-entry-form.tsx calls saveEquipmentEntry
  - add-equipment-modal.tsx calls createEquipoFromField
  - work-type-toggle.tsx renders preventivo/correctivo toggle
  </verify>
  <done>
  Complete report form with Realtime subscription, full equipment section (list entries, add existing equipment, add new from field, edit/save/remove entries), work type toggle, and mobile UX.
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit` — zero TypeScript errors
- `npm run build` — full production build succeeds
- Navigation flow works: /tecnico (folio list) -> /tecnico/folios/[id] (creates report, redirects) -> /tecnico/reporte/[id] (full form)
- Tab bar hidden on report pages, visible on folio list
- Equipment entries can be added, edited, saved, and removed via server actions
- New equipment modal creates equipment with revisado=false
- Realtime banner appears when subscribed (functional when Supabase Realtime is configured)
</verification>

<success_criteria>
- Technician folio list shows assigned folios with today's report status
- Folio detail auto-creates/finds today's report and redirects
- Report form renders with folio context, team info, and equipment section
- Equipment entries: add from dropdown, save individually, remove with optimistic UI
- Add equipment modal creates field equipment flagged for review
- Work type toggle switches between preventivo/correctivo
- Realtime refresh banner wired up
- Tab bar hidden during report editing
- Pre-populated equipment from previous day's report (auto-draft)
</success_criteria>

<output>
After completion, create `.planning/phases/03-technician-reporting/03-02-SUMMARY.md`
</output>
